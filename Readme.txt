
You can configure the properties file with

keyname=xml source filename,xml destionation file name,Element name,Element updated value

The source file folder is src/test/resources/input
The destination file folder is src/test/resources/output

The entry class is XpathUpdater.java

You can run it as a Java application.